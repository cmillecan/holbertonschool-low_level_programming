#include "holberton.h"
#include <stdio.h>

/**
 * main - Entry point
 * @argc: argument count
 * @argv: array of pointers to a string
 * Return: int
 */
int main(int __attribute__((__unused__)) argc, char *argv[])
{
	printf("%s\n", argv[0]);

	return (0);
}
